const model = require('../models');

class MainController {
    static async test(ctx){
        let test = 1;
        // let res = await model.new_table.findAll();


        ctx.body = 1;



    }
}

module.exports = MainController;